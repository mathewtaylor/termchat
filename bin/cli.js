#!/usr/bin/env bun
/**
 * TermChat CLI Entry Point
 *
 * This script is executed when users run 'termchat' or 'tc' from the command line.
 * It requires Bun runtime to be installed on the system.
 */

import '../src/index.ts';
